@using Opc.Ua.Cloud.Publisher.Pages

@{
    ViewData["Title"] = "Diagnostics";
}

<div class="page-header">
    <h1 class="page-title">@ViewData["Title"]</h1>
    <div class="page-subtitle">Live diagnostics and runtime metrics for the publisher.</div>
</div>

<div class="app-card uacp-diagnostics">
    <component>@(await Html.RenderComponentAsync<Chart>(
        RenderMode.Server,
        new { }).ConfigureAwait(false))</component>
</div>
