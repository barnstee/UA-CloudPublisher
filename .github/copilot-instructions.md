# Copilot Instructions

## Project Guidelines
- Always treat compiler warnings as errors: proactively fix all compiler warnings (including obsolete-API/deprecation warnings like CS0618) rather than leaving them, and verify a clean build with zero warnings.